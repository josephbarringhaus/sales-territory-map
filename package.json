{
  "name": "territory-map-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/territory-map",
    "build": "npm run build --workspace=apps/territory-map",
    "start": "npm run start --workspace=apps/territory-map",
    "lint": "npm run lint --workspace=apps/territory-map"
  }
}

